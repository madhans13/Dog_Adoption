apiVersion: apps/v1
kind: Deployment
metadata:
  name: dog-adoption-frontend
  namespace: dog-adoption
  labels:
    app: dog-adoption-frontend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: dog-adoption-frontend
  template:
    metadata:
      labels:
        app: dog-adoption-frontend
    spec:
      containers:
        - name: dog-adoption-frontend
          image: madhan13/dog-adoption-frontend:v19
          imagePullPolicy: Always
          env:
            - name: RUNTIME_API_BASE_URL
              value: "https://dog-adoption.dev"
          ports:
            - containerPort: 8080
              